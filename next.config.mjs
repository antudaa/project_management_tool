/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["links.papareact.com", "federalnewsnetwork.com"],
        // hostname: ["federalnewsnetwork.com"]
    }
};

export default nextConfig;
